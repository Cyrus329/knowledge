# v20 完整图片修复版

这版重点修复：手机/GitHub 打开时图片不显示。

必须保留这些文件夹：

- images
- exam_video_v15
- pdfs

不能只上传 index.html。

如果用 GitHub Pages：把本压缩包解压后，里面所有文件和文件夹一起上传到仓库根目录。看到仓库里有 `images`、`exam_video_v15`、`pdfs` 三个文件夹，图片才会正常显示。

本版仍然保留 v19 的瘦身排版：废话说明已删除，首页统计/任务改成折叠分区，考点必背默认直接看填空。
